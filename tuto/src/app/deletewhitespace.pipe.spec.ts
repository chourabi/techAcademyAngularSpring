import { DeletewhitespacePipe } from './deletewhitespace.pipe';

describe('DeletewhitespacePipe', () => {
  it('create an instance', () => {
    const pipe = new DeletewhitespacePipe();
    expect(pipe).toBeTruthy();
  });
});
